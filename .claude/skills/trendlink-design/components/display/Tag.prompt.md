Outlined chip for categories, blog tags, and filter bars. Set `active` for selected, pass `onRemove` for a removable filter chip.

```jsx
<Tag active>勞動法令</Tag>
<Tag onClick={...}>績效考核</Tag>
<Tag onRemove={() => ...}>高雄</Tag>
```
